class NotificationData {
  final String title;
  final String message;
  final DateTime time; // Updated to store DateTime

  NotificationData({
    required this.title,
    required this.message,
    required this.time, // Updated to store DateTime
  });
}